# 项目约定

## 语言

- 面向用户的回复与项目文档默认使用简体中文。
- 代码标识符、命令、固定文件名、API 字段及无法自然翻译的专有名词保留英文。

## Agent skills

### Issue tracker

需求、规格与实施任务使用 GitHub Issues 管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

Issue 分流使用 Matt Pocock 技能的默认标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

本项目采用 single-context：领域术语记录在根目录 `CONTEXT.md`，架构决策记录在 `docs/adr/`。详见 `docs/agents/domain.md`。
